spring.application.name=toolmanagementsystem

# Dynamic Port for Render Deployment (defaults to 8080 locally)
server.port=${PORT:8080}

# Cloud Database Connection
# Supports Railway/Aiven/other account env naming with a local fallback.
spring.datasource.url=${DB_URL:${DATABASE_URL:${MYSQL_URL:jdbc:mysql://localhost:3306/tool_management}}}
spring.datasource.username=${DB_USER:${DB_USERNAME:${MYSQLUSER:root}}}
spring.datasource.password=${DB_PASSWORD:${DB_PASS:${MYSQLPASSWORD:admin}}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# CORS Configuration
cors.allowed-origins=https://tool-management-system-two.vercel.app

# Spring Mail Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL}
spring.mail.password=${EMAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# JWT settings
jwt.secret=${JWT_SECRET}
jwt.expirationMs=86400000
